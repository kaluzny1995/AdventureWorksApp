export class ImageDisplayData {
    public readonly title: string;
    public readonly src: string;

    constructor(title: string, src: string) {
        this.title = title;
        this.src = src;
    }
}
